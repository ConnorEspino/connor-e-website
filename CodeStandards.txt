Indenting:
    No tabs
    4 spaces
    2 in .css

CSS Class Names:
    CapitalizeEveryWord

Quotations:
    'Single Quotes unless escaped with \"'

Variables:
    kCamel for constant variables
    mCamel for member variable
    lCamel for localStorage variables

    camelCase for css className

    Variables in any scope should be listed: 
        const firstConstVar
        const secondConstVar
        let firstLetVar
        let secondLetVar

If-Else:
    if () {
        Code
    }
    else {
        Code
    }